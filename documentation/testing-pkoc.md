
spec

settings - use main settings
add ptr in context to pkoc specifi context
save values in (my root)/osdp-pkoc-context.json

parser
return 2 levels lists of 8
error if more than 2 levels
status
ok
too long
unknown tags
too many levels

hex binary prims object to library

